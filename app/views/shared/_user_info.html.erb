<a href="<%= user_path(current_user) %>">
  <%= gravatar_for(current_user) %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "查看我的blog", current_user %>
</span>
<span>
  <%= pluralize(current_user.blogs.count, "blog") %>
</span>